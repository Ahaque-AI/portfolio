/**
 * Deprecated. The interactive chat panel was replaced in the editorial
 * rebuild by `src/components/sections/Faq.astro` — an inline Q&A section
 * with a search input and accordion, all plain DOM (no React island).
 *
 * This file is kept temporarily so `astro check` doesn't lose track of
 * the directory; it can be removed once the rebuild lands.
 */
export default function Chatbot() {
  return null;
}