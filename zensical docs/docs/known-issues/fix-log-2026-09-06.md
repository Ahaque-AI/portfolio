# Smoother orbits and guided-flight scene foundation

## Symptom and cause

Orbit motion felt uneven. The introduction traveler used wall time, jumping
ahead after hidden/offscreen pauses. Map parallax used a fixed interpolation
per frame, changing response with refresh rate. Its signal wrapped from the
route end directly to the start. Introduction tilt affected only the SVG,
separating its annotation from the WebGL geometry.

## Resolution

Use a paused elapsed clock for the traveler, reset map timestamps on resume,
and use elapsed-time exponential damping for map parallax. The signal now
eases along the route and returns continuously. Apply introduction tilt to
the shared SVG/canvas wrapper, including the reduced-motion override.

Implemented [guided-flight Step 02](../plans/guided-flight-solar-system.md):
reusable solar-system and rocket geometry, three planet stops, deterministic
stars, sparse comet, mouse parallax, visibility/reduced-motion handling and
resource disposal. The Step 03 overlay is not part of this change.

## Verification

- `pnpm test`: 18 tests passed, including scene geometry and complete disposal.
- Strict frontend static audit: no findings.
- `pnpm check`: 0 errors, warnings or hints.
- `pnpm build`: passed, five static routes; Vite reports a chunk over 500 kB.
- Browser smoothness, responsive framing and interaction remain unverified;
  the owner performs manual browser tests under the frontend rules.

## Guided spaceship tutorial and About, Steps 03 and 04

The guided-flight trigger still opened the old step dialog because Step 02
only delivered a scene module. The trigger now intent-loads that scene inside
a full-screen native dialog. A spline carries the rocket through Arrival,
Work and About; captions lead to a real About link. Native dialog behavior
owns focus containment, Escape and background inertness. Close/skip release
the scene and restore focus. Stale imports cannot mount after closing.
WebGL failure retains readable captions and reduced motion skips spatial travel.

Added the static About page with five evidence-based work summaries, reported
results and tools. About is an available map link; Work stays muted.

Verification: 22 unit tests passed, Astro check reports no diagnostics, strict
frontend audit has no findings, and the production build emits all six routes.
Vite still reports a chunk over 500 kB. Browser framing, keyboard behavior and
motion feel remain owner checks. Step 05 route transitions remain deferred.

## Guided experience and asteroid encounters

The guided flight felt like a conventional step dialog and the ship resembled a
pencil. The flight now uses asteroid encounters at every stop, a bounded data
stream, and a compact exploration craft with cockpit, wings and twin engines.
The first map visit spotlights the launch control until the visitor starts the
flight. Its session-only marker contains no personal information. Shared orbital
ring feedback now applies to controls and destinations, while every internal
route uses the WebGL orbit transition where motion is available.

Verification: scene tests assert asteroid fields, encounter motion, ship parts,
route spline finiteness and resource disposal. Manual first-visit focus and
browser motion review remain with the owner.

## Guided game controls and resume presentation

The flight still resembled a tutorial because its next, back and landing actions
were visible in the game. It now advances each asteroid encounter automatically
and keeps only Back to map. The launch control now has dark text on a solid
citron background. Its focus veil was reduced to a low-opacity 1.8px blur so
the route map stays visible.

The prior CV was a portfolio reading page with a rail and many links. It now
uses the owner-supplied one-page PDF as its layout reference: a self-contained
fitted A4 resume sheet with no site chrome or resume navigation. Narrow
viewports preserve readable document scrolling.
